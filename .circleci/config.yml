version: 2.1

orbs:
  node: circleci/node@0.0.4

jobs:
  build:
    executor: node/node
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run:
                name: install dependency
                command: yarn
      - run:
          name: build docs
          command: yarn build
      - persist_to_workspace:
          root: .
          paths:
              - ./public
  deploy:
    machine: true
    steps:
      - attach_workspace:
          at: .
      - add_ssh_keys:
          fingerprints:
            - "81:a6:53:45:aa:0b:c9:bc:e4:e0:08:1d:59:1f:b5:24"
      - run:
          name: initialize git
          command: |
            git init .
            git config --global user.name "circle ci"
            git config --global user.email "odan3240@gmail.com"
          working_directory: ./public
      - run:
          name: deploy to gh-pages
          command: |
            git add ./
            git commit -m 'Update [ci skip]'
            git push -f git@github.com:odanado/vuepress-sandbox.git master
          working_directory: ./public

workflows:
  build-and-deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
          filters:
            branches:
              only: source